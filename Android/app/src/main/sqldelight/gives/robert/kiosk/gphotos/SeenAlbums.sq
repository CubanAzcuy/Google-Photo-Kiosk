CREATE TABLE SeenAlbums (
  id TEXT NOT NULL,
  title TEXT NOT NULL,
  cover_photo_url TEXT NOT NULL
);

selectAll:
SELECT *
FROM SeenAlbums;

insert:
INSERT INTO SeenAlbums(id, title, cover_photo_url)
VALUES (?, ?, ?);
